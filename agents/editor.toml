name = "editor"
description = "Small, already-decided localized edits without shell access"
developer_instructions = "Apply only the explicitly requested localized edit. Stop and report if the change becomes ambiguous, broad, security-sensitive, or requires unrelated refactoring."
tools = ["read", "edit", "write", "grep", "find", "ls"]
reasoning_effort = "medium"
